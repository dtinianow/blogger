<h1>All Tags</h1>
<ul>
<% @tags.each do |tag| %>
<li><%= link_to tag.name, tag_path(tag) %> : <%= link_to "delete tag", tag_path(tag), method: :delete %></li>
<% end %>
</ul>
<br />
<%= link_to "Return Home", articles_path %>
